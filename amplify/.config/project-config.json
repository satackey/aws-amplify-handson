{
    "projectName": "aws-amplify-handson",
    "version": "3.0",
    "frontend": "javascript",
    "javascript": {
        "framework": "react",
        "config": {
            "SourceDir": "frontend/src",
            "DistributionDir": "frontend/build",
            "BuildCommand": "docker-compose run frontend_dev yarn build",
            "StartCommand": "docker-compose up"
        }
    },
    "providers": [
        "awscloudformation"
    ]
}